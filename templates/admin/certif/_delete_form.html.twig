<form method="post" action="{{ path('certif_delete', {'id': certification.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ certification.id) }}">
    <button class="btn form-submit delete_button">Delete</button>
</form>
